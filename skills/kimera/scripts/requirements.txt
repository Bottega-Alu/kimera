# Kimera 2.0 runtime - all versions pinned.
# playwright is pinned to the release whose bundled Chromium revision (1208 /
# Chromium 145) is already present on this machine, so `playwright install
# chromium` is a no-op instead of a ~150 MB download.
markdown==3.10.3
nh3==0.3.7
playwright==1.58.0
pymupdf==1.27.2
