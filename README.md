# Kimera(R) 2.0 — Markdown to print-ready document

> **EN** Write Markdown, get a self-contained HTML document and a PDF whose page
> geometry has actually been measured.
> **IT** Scrivi Markdown, ottieni un documento HTML autonomo e un PDF la cui
> impaginazione e stata misurata davvero.

[![License: CC BY-NC 4.0](https://img.shields.io/badge/License-CC%20BY--NC%204.0-lightgrey.svg)](https://creativecommons.org/licenses/by-nc/4.0/)
[![Buy Me an Aperitivo](https://img.shields.io/badge/Buy%20Me%20an%20Aperitivo-FFDD00?style=flat&logo=buy-me-a-coffee&logoColor=black)](https://buymeacoffee.com/bottega.alu)

---

## Why "Kimera"?

**Kimera** is the Italian spelling of *Chimera* — the mythical creature that
fuses different beings into one. Kimera fuses a Markdown source, a print
stylesheet and a real quality gate into a single deliverable.

---

## What changed in 2.0

Version 1 asked the model to emit HTML and CSS, to guess how much content fits on
a page in pixels, and to place the footer by hand. Version 2.0 removes all of
that from the model's job.

| Concern | v1 | 2.0 |
|---|---|---|
| Layout | HTML + CSS regenerated by the model per document | one stylesheet shipped as an asset |
| Pagination | fixed-height pages, `overflow: hidden`, manual pixel budgets | Chromium's print engine, CSS `@page` |
| Footer | `position: absolute` inside each page div | `@page` margin boxes, `counter(page) / counter(pages)` |
| Page numbers | hardcoded per page | CSS counters |
| Quality gate | a QA script rewritten per document | `verify.py`, run on the PDF that was actually produced |
| Skill instructions | ~24 KB | ~2.5 KB |

The model writes Markdown. Everything else is code.

---

## Architecture

```
skills/kimera/                  the installable skill
  SKILL.md                      flow and constraints only (~2.5 KB)
  scripts/render.py             CLI: Markdown -> .md.html [-> .pdf + report]
  scripts/verify.py             PDF checks with PyMuPDF, importable and CLI
  scripts/setup.ps1             creates the dedicated venv, writes python.txt
  scripts/requirements.txt      pinned dependencies
  assets/kimera.css             screen + print stylesheet
  assets/template.html          HTML skeleton (string.Template)
  references/authoring.md       supported syntax, callouts, limits
  references/printing.md        verification, diagnosis, manual printing
tests/run_tests.py              6 fixtures x A4/Letter/Legal, no pytest
examples/demo-it.md             Italian demo (source, HTML and PDF)
```

Pipeline: Markdown source -> Python-Markdown (`meta, tables, fenced_code,
sane_lists, admonition, footnotes, def_list, abbr`) -> `nh3` allowlist ->
`string.Template` -> one self-contained HTML file with the CSS embedded and a
generated `@page` block -> Chromium `page.pdf()` -> PyMuPDF verification.

### Stack

Python 3.10+, [Python-Markdown](https://python-markdown.github.io/),
[nh3](https://pypi.org/project/nh3/) (Ammonia HTML sanitiser),
[Playwright](https://playwright.dev/python/) driving Chromium 131+, and
[PyMuPDF](https://pymupdf.readthedocs.io/). Versions are pinned in
`skills/kimera/scripts/requirements.txt`.

Dependencies are installed **once** into a dedicated virtualenv
(`%LOCALAPPDATA%\kimera\venv` by default) and reused by every installation of the
skill. `render.py` bootstraps itself towards that interpreter, so it can be
launched with any Python on the machine.

---

## Setup

```powershell
powershell -ExecutionPolicy Bypass -File skills/kimera/scripts/setup.ps1
```

Creates the virtualenv, installs the pinned dependencies, makes sure Chromium is
available to Playwright, checks the imports and the browser version, and records
the interpreter path in `skills/kimera/python.txt` (gitignored, machine-specific).

Options: `-Python -3.11` (or an absolute path), `-VenvPath ...`, `-Force`.

---

## CLI

```bash
python skills/kimera/scripts/render.py SOURCE.md [options]
```

| Option | Meaning |
|---|---|
| `--out PATH` | HTML output, default `SOURCE.md.html` |
| `--verify` | also produce the PDF and check it |
| `--pdf PATH` / `--no-pdf` | PDF location, or verify in a temp file and keep nothing |
| `--paper A4\|Letter\|Legal` | default A4 |
| `--lang it\|en` | footer and hint strings, default `it` |
| `--title --subtitle --project --author --date --label` | header metadata |
| `--confidential "text"` | one confidentiality line at the end |
| `--report PATH` / `--json` | machine-readable report |

Metadata can also come from a front-matter block at the top of the Markdown
(`Title:`, `Subtitle:`, `Project:`, `Author:`, `Date:`, `Label:`, `Lang:`,
`Paper:`, `Confidential:`). CLI flags win. Nothing is mandatory and nothing is
invented: a field that is not supplied simply does not appear.

Exit codes: `0` fine, `1` a check failed, `2` internal error, `3` runtime missing.

Without `--verify` the tool writes the HTML only and reports `NOT VERIFIED`.

### What gets verified

Measured on the real PDF, not estimated: paper size within 0.5 mm, no blank
pages, every text block inside the text area, the footer present on every page
and clear of the body, every 4+ character word of the source present in the
output, no leaked renderer token, Chromium 131 or newer.

Failures come with a diagnosis that says what to change **in the content**.
Margins and font sizes are never the answer.

---

## Tests

```bash
python tests/run_tests.py
```

Six fixtures — realistic short document, 80-row table, a single table row taller
than a page, a paragraph longer than a page, a 200-line code block, and long
URLs / unbreakable strings / accented and CJK characters — each rendered on A4,
Letter and Legal. Every fixture is seeded with unique `MRK-####` markers that
must appear **exactly once** in the PDF, which catches both dropped and
duplicated content. Results, timings and page PNGs land in `tests/results/`.

---

## Layout rules

- Paper A4 (default), Letter or Legal. Margins 16 mm top, 18 mm sides, 22 mm
  bottom, set by CSS `@page`.
- Footer inside the bottom margin, 5 mm below the body, built from `@page` margin
  boxes: project or title on the left, `Pagina N di M` on the right.
- Body 10.5 pt, tables 9.25 pt, code 8.75 pt, system font stack.
- Table headers repeat on every page. Paragraphs, lists, code blocks and table
  rows may all continue on the next page.
- Long URLs and unbreakable strings wrap instead of overflowing.
- No fixed page heights, no `overflow: hidden` on content, no absolutely
  positioned footers, no pixel budgets.

---

## Limits

- **No raw HTML** in the source: tags are stripped by the sanitiser, their text
  is kept.
- **No images** in this version.
- **No syntax highlighting** in code blocks.
- **No custom classes, ids or inline styles.**
- **No guaranteed fidelity in Word, LibreOffice or Google Docs.** The output is
  standard HTML and those applications will open it, but the print layout is only
  guaranteed in Chromium. Send the PDF.

---

## Printing by hand

The `.pdf` produced by `--verify` is already paginated — print that when you can.

To print the `.md.html` directly: open it in an up-to-date Chromium-based
browser, `Ctrl+P`, paper size equal to `--paper`, scale 100%, margins **Default**
(the `@page` rule already provides them), and the browser's own headers and
footers turned **off**.

---

## Installation

### Claude Code plugin

```bash
/plugin marketplace add Bottega-Alu/kimera
/plugin install kimera
```

### Global or project skill

```bash
# global
cp -r skills/kimera ~/.claude/skills/kimera
# per project
cp -r skills/kimera .claude/skills/kimera
```

Then run `setup.ps1` from the installed copy once.

---

## License

| Use case | License | Cost |
|---|---|---|
| Personal, educational, open-source | CC BY-NC 4.0 | free, with attribution |
| Commercial | Aperitivo License | [buy me an aperitivo](https://buymeacoffee.com/bottega.alu) |

Attribution for non-commercial use:

```
Based on Kimera(R) by Francesco Trani
https://github.com/Bottega-Alu/kimera
```

**Kimera(R)** is a trademark of Francesco Trani.

---

## Author

**Francesco Trani** — [@Bottega-Alu](https://github.com/Bottega-Alu) —
[buymeacoffee.com/bottega.alu](https://buymeacoffee.com/bottega.alu)

---

## Changelog

### 2.0 (2026-09-08)
- Rendering moved out of the model into `scripts/render.py`; SKILL.md down from
  ~24 KB to ~2.5 KB.
- Pagination delegated to Chromium's print engine via CSS `@page`; fixed page
  heights, `overflow: hidden`, absolute footers and pixel budgets removed.
- Footer and page numbers via `@page` margin boxes and CSS counters.
- `verify.py`: seven checks measured on the produced PDF, with content-side
  diagnosis instead of open-ended correction loops.
- Test suite over 6 fixtures x 3 paper sizes with uniqueness markers.
- Metadata optional; no invented author, date, metrics or confidentiality notice.
- Dropped the claims of zero dependencies and of Word/LibreOffice editability,
  and the "Margins: None" print instruction.

### 1.0 (2026-02-21)
- Initial release: model-side HTML template, 5-colour palette, A4/Letter/Legal.
